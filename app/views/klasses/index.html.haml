%h1 Классы
- unless @klasses.empty?
  %table
    %tr
      %th Имя (номер)
      %th Год обучения
      %th
      %th
      %th
    - @klasses.each do |f|
      %tr
        %td=f.name
        %td=f.level
        %td=link_to 'Изменить', edit_klass_path(f)
        %td=link_to 'Удалить', f, :method => :delete, :data => {confirm: "Вы уверены?"}
        %td=link_to 'Предметы', klass_subject_relations_path(:params => {:klasses_id => f.id })
- else
  .empty Пустой список

= link_to 'Новый класс', new_klass_path()